import { Router } from "express"

const gradesRouter: Router = Router()

gradesRouter.post('/addGrade', )

gradesRouter.put('/putGrade', )

gradesRouter.get('/:id', )

gradesRouter.get('/classAverage', )

gradesRouter.post('/allGrades', )

export default gradesRouter